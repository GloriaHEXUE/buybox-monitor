# BuyBox Monitor 项目对话整理

## 项目背景

我们在本地搭建了一个 `BuyBox Monitor` 网站，用来处理和查看以下数据：

- 每日上传的 Keepa 数据源
- 不定期上传的映射信息
- SKU / ASIN 监控清单

目标是让页面可以通过在线检索 `运营`、`SKU`、`ASIN` 来查看：

- 对应 KMASIN 与竞对 ASIN
- 价格变化
- 排名变化
- Buy Box 丢失与恢复
- 缺失数据、重复监控、趋势预警等提醒

同时尽量采用本地规则解析，不依赖 token 消耗。

---

## 你提出的核心需求

### 1. 基础数据能力

- 自动读取你提供的 Excel 表内容
- 将映射信息、ASIN、运营等初始数据录入页面
- 上传 Keepa 后记录历史数据
- Keepa 历史保留天数从 3 天改为 5 天
- 解析 Keepa 时尽量避免卡顿

### 2. 检索体验

- 检索区域拆成多个栏位
- 支持分别搜索：
  - 人名 / 运营
  - SKU
  - ASIN
  - 品牌 / 标题 / 组别 / 账号
- 人名、ASIN、SKU 使用下拉候选
- 检索结果用表格展示
- ASIN 详情放到检索结果下方

### 3. 页面结构

- SKU 监控清单和映射信息默认收起
- 点击左侧按钮时再展开维护区
- 缺少 Keepa、重复监控等摘要信息尽量做成简洁提示

### 4. 数据展示规则

- 类型、品牌放在 ASIN 左边
- KMASIN 标签底色为红色
- 竞对 ASIN 标签底色为绿色
- 同一个 SKU 下的 ASIN 用分组方式框起来

### 5. 排名与价格变化展示

- 当价格或排名有变化时，在数值旁边显示箭头
- 价格变化：
  - 涨价用红色
  - 降价用绿色
- 排名变化最终规则改为：
  - 排名上升用红色
  - 排名下降用绿色
  - 不显示 `+ / -`
  - 只显示箭头、变化值、百分比
- 页面里所有“排名变化”的列名统一整理为 `排名`

### 6. Buy Box 预警

- 重点只监控 KMASIN 的 Buy Box 变化
- 重点关心：
  - 丢失购物车
  - 恢复购物车
- 页面拆成两个板块：
  - 左边 Buy Box 丢失
  - 右边 Buy Box 恢复

### 7. 预警与异常

- 缺少 Keepa 数据：
  - 只显示缺少多少条
  - 支持下载缺失清单
- 缺少映射信息：
  - 支持查看说明
  - 支持去补映射
- 重复监控关系：
  - 定义为 `同运营 + 同SKU + 同ASIN类型 + 同ASIN`
  - 单纯 ASIN 重复但主体不同，不算重复
  - 页面上只显示重复数量
  - 支持下载“删减后可重传”的表格
- 价格异常：
  - 需要看板展示
  - 需要筛选运营 / SKU / 品类
  - 需要下载表格
- 排名趋势：
  - 最终不做单独排名看板
  - 改成预警中心中的近 5 天趋势预警表

### 8. 上传和维护能力

- 三类文件上传都要提供模板
  - 每日 Keepa 数据源
  - 映射信息
  - SKU / ASIN 监控清单
- 映射信息、SKU 监控清单支持在线新增、修改、删除
- 上传后要有“解析说明 / 报错说明”

### 9. 部署和使用方式

- 你希望后续部署网页后尽量通过公式、规则、字符匹配归类
- 避免每次用网页时消耗 token

---

## 已完成的功能整理

### 数据导入与持久化

- 已支持上传并解析 Keepa、映射、监控清单 Excel
- 已支持将初始表格数据作为种子数据载入
- Keepa 历史记录已改为保留最近 5 天
- 同一天重复上传 Keepa 时，会替换当天快照

### Keepa 解析优化

- 避免完整读取超多列造成卡顿
- 改为只提取关键字段：
  - ASIN
  - 标题
  - 品牌
  - 价格
  - 排名
  - Buy Box
  - Coupon
  - Prime Price
  - 图片

### 检索与展示

- 顶部检索已拆分为多个输入框
- 支持运营 / SKU / ASIN / 关键词分别检索
- 支持下拉候选
- 检索结果表格支持按 SKU 进行分组展示
- 选中后可在下方查看 ASIN 详情

### 标签与分组视觉

- KMASIN 使用红色标签
- 竞对 ASIN 使用绿色标签
- 同一个 SKU 的相关 ASIN 做了组边框和层级展示

### 维护区

- SKU 监控清单、映射信息已放在左侧按钮控制的折叠区域
- 支持在线新增、编辑、删除、导出

### 预警中心

- 缺少 Keepa：显示数量并支持导出
- Buy Box：
  - 已拆成“丢失 / 恢复”两个区域
- 重复监控：
  - 已简化成数量摘要
  - 支持下载“删减后重传表”
- 近 5 天排名趋势预警：
  - 已展示运营、SKU、ASIN、类型、起始排名、当前排名、趋势、变化幅度、连续天数

### 价格异常看板

- 支持按运营 / SKU / 品类筛选
- 支持表头排序：
  - 价格变动最大 / 最小
  - 排名差异最大 / 最小
- 展示方式已调整为：
  - `价格`列中直接显示当前价格 + 箭头 + 变化值 + 百分比
  - `排名`列中直接显示当前排名 + 箭头 + 变化值 + 百分比
- KMASIN 父行与竞对 ASIN 子行上下对照显示

### 上传说明

- 页面已提供上传模板下载
- 页面已提供上传后说明区：
  - 本次导入数
  - 解析说明
  - 报错说明

---

## 当前重要业务规则

### 1. 重复监控判定规则

只有满足以下条件才算重复监控：

- 同运营
- 同 SKU
- 同 ASIN 类型
- 同 ASIN

以下情况不算重复：

- 不同运营监控同一个竞对 ASIN
- 不同 SKU 监控同一个竞对 ASIN

### 2. Buy Box 变化规则

重点只看 KMASIN 的 Buy Box 状态变化：

- 丢失购物车
- 恢复购物车

不再把所有卖家切换都视作重点预警。

### 3. 映射缺失规则

只有在监控清单本身缺少运营 / 组别 / 账号，并且又找不到映射信息时，才提示缺少映射。

### 4. 排名变化规则

当前页面最终使用的口径是：

- 排名上升：红色
- 排名下降：绿色
- 不显示正负号
- 通过箭头和数值展示变化

---

## 当前页面状态

本地项目目录：

- `C:\Users\Lenovo\Documents\buy\buybox-monitor`

本地访问地址：

- [http://127.0.0.1:5173/](http://127.0.0.1:5173/)

最近一次状态确认：

- 页面可正常打开
- `build` 通过
- `lint` 通过

---

## 最近一次明确调整

你最新提出并已完成的要求是：

- 整个页面里的排名标志全部调整
- 排名上升改为红色
- 排名下降改为绿色
- 不再显示加减号

这部分已经同步修改到：

- 检索结果表格
- 价格异常看板
- 近 5 天排名趋势预警

---

## 后续可继续优化的方向

- 给检索结果表头也加入排序 / 筛选菜单
- 优化价格异常看板图表 tooltip 的信息密度与样式
- 增加更清晰的导出说明文案
- 继续压缩前端打包体积
- 增加正式部署方案，比如部署到 Hugging Face Spaces 或其他静态托管平台

---

## 一句话总结

这个项目已经从“单纯上传表格查看数据”，逐步整理成了一个以本地规则解析为核心、支持监控维护、异常看板、Buy Box 预警、重复清理和趋势查看的运营监控网站。
