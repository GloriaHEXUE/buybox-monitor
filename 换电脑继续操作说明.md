# 换电脑继续操作说明

## 这份说明是做什么的

这份文件是给以后换电脑、换登录地点时继续使用 `buybox-monitor` 项目准备的。

当前项目目录：

- `C:\Users\Lenovo\Documents\buy\buybox-monitor`

本地开发地址通常是：

- `http://127.0.0.1:5173/`

---

## 一、先说结论

如果你以后换一台电脑，想继续用这个项目，最稳的做法是：

1. 把项目上传到 GitHub
2. 新电脑把项目下载下来
3. 安装 Node.js
4. 安装依赖
5. 启动网站

如果聊天记录没同步，只要这个项目代码和这份说明在，你也可以继续接着做。

---

## 二、当前项目里最重要的文件

### 代码文件

- `src/`
- `public/`
- `package.json`
- `package-lock.json`
- `vite.config.ts`
- `index.html`

### 项目记录文件

- `Obsidian-项目对话整理.md`
- `换电脑继续操作说明.md`

---

## 三、上传到 GitHub 的最简单方法

当前这台电脑的命令行里没有可用的 Git，所以最简单的方法是：

### 方法 A：直接用 GitHub 网页上传

#### 第 1 步：打开 GitHub

打开：

- [https://github.com](https://github.com)

登录你的 GitHub 账号。

#### 第 2 步：新建仓库

右上角点：

- `New repository`

建议这样填：

- Repository name: `buybox-monitor`
- 公开或私有：都可以，建议先选 `Private`

然后点：

- `Create repository`

#### 第 3 步：上传项目文件

进入新仓库后，点：

- `uploading an existing file`

然后把下面这些文件和文件夹拖进去：

- `src`
- `public`
- `.gitignore`
- `.oxlintrc.json`
- `index.html`
- `package.json`
- `package-lock.json`
- `README.md`
- `vite.config.ts`
- `tsconfig.app.json`
- `tsconfig.json`
- `tsconfig.node.json`
- `Obsidian-项目对话整理.md`
- `换电脑继续操作说明.md`

### 不要上传这些

- `node_modules`
- `dist`

因为：

- `node_modules` 太大，不需要上传
- `dist` 是构建产物，可以以后自动重新生成

#### 第 4 步：提交上传

页面下方填写提交说明，例如：

```text
init buybox monitor project
```

然后点：

- `Commit changes`

这样项目就上传到 GitHub 了。

---

## 四、换到新电脑后怎么继续

### 第 1 步：打开 GitHub 仓库

找到你保存项目的仓库，比如：

- `https://github.com/你的用户名/buybox-monitor`

### 第 2 步：下载代码

如果新电脑也没有 Git，可以直接点：

- `Code`
- `Download ZIP`

把 ZIP 下载到新电脑后解压。

例如解压到：

- `C:\Users\Lenovo\Documents\buy\buybox-monitor`

---

## 五、新电脑需要安装什么

你至少需要安装：

### 1. Node.js

官网：

- [https://nodejs.org/](https://nodejs.org/)

建议安装：

- `LTS` 版本

安装完成后，打开 PowerShell，输入：

```powershell
node -v
npm -v
```

如果能显示版本号，就说明装好了。

### 2. Git（可选）

如果你以后还想继续同步 GitHub，建议装 Git：

- [https://git-scm.com/downloads](https://git-scm.com/downloads)

如果只是临时运行网页，不装 Git 也可以。

---

## 六、新电脑怎么启动项目

### 第 1 步：打开 PowerShell

进入项目目录：

```powershell
cd C:\Users\Lenovo\Documents\buy\buybox-monitor
```

### 第 2 步：安装依赖

第一次打开项目时执行：

```powershell
npm install
```

### 第 3 步：启动本地网站

```powershell
npm run dev
```

看到类似地址后，复制到浏览器打开：

```text
http://127.0.0.1:5173/
```

---

## 七、如果只想检查代码有没有问题

可以运行：

```powershell
npm run build
npm run lint
```

作用：

- `build`：检查项目能不能正常打包
- `lint`：检查代码格式和潜在问题

---

## 八、数据和聊天记录要怎么保留

### 1. 代码靠 GitHub 保存

项目代码、页面结构、功能修改，主要靠 GitHub 保存。

### 2. 需求和规则靠 Markdown 保存

这两个文件很重要：

- `Obsidian-项目对话整理.md`
- `换电脑继续操作说明.md`

### 3. 表格数据要单独保留

像这些 Excel 文件建议单独备份：

- Keepa 数据源
- 映射信息
- SKU / ASIN 监控清单

因为它们不一定适合长期直接放在代码仓库里。

---

## 九、如果以后要继续部署到 Hugging Face

建议顺序：

1. 先上传 GitHub
2. 确认本地 `npm run build` 没问题
3. 再把 GitHub 仓库连接到 Hugging Face Spaces

你这个项目适合：

- `Static Space`

不是：

- `Gradio`

---

## 十、以后最省心的工作流

建议以后固定这样做：

1. 本地改代码
2. 测试页面
3. 上传 GitHub
4. 需要部署时再同步到 Hugging Face
5. 需求变动记到 `Obsidian-项目对话整理.md`

---

## 十一、当前这次的特别说明

当前这台电脑里，命令行没有识别到 `git`，所以没法直接帮你执行 `git push`。

这不影响项目本身，只是表示：

- 现在更适合先用 GitHub 网页上传
- 或者先安装 Git，再做命令行上传

---

## 十二、一句话版

以后换电脑时，只要拿到 GitHub 仓库、安装 Node.js、执行 `npm install` 和 `npm run dev`，这个网站就能继续跑起来。
